import 'package:flutter/material.dart';

class PaddingItems {
  static const EdgeInsets horizontalPadding = EdgeInsets.symmetric(
    horizontal: 15,
  );
  static const EdgeInsets horizontalPadding2x = EdgeInsets.symmetric(
    horizontal: 30,
  );
  static const EdgeInsets verticalPadding = EdgeInsets.symmetric(
    vertical: 10,
  );
  static const EdgeInsets bottomPadding = EdgeInsets.only(
    bottom: 30,
  );
  static const EdgeInsets topPadding = EdgeInsets.only(
    top: 30,
  );
  static const EdgeInsets topPadding2x = EdgeInsets.only(
    top: 30,
  );

  static const EdgeInsets rightPadding = EdgeInsets.only(right: 15);
  static const EdgeInsets zeroPadding = EdgeInsets.zero;
  static const EdgeInsets allPadding = EdgeInsets.all(10);
  static const EdgeInsets leftPadding = EdgeInsets.only(left: 15);
}
